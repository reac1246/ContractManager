<Window x:Class="ContractManager.Views.FirstAdminDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="初期セットアップ - 管理者アカウントの作成" 
        MinHeight="350" Width="450"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="管理者が存在しません。最初の管理者アカウントを作成してください。" 
                   TextWrapping="Wrap" Margin="0,0,0,16" Foreground="{StaticResource TextBrush}"/>
        
        <!-- Username -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="管理者ID" Style="{StaticResource FormLabel}"/>
            <TextBox x:Name="UsernameBox" Style="{StaticResource ModernTextBox}"/>
        </StackPanel>
        
        <!-- Password -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="パスワード" Style="{StaticResource FormLabel}"/>
            <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernPasswordBox}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Text="パスワード（確認用）" Style="{StaticResource FormLabel}"/>
            <PasswordBox x:Name="PasswordConfirmBox" Style="{StaticResource ModernPasswordBox}"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="作成してログイン" 
                    Style="{StaticResource PrimaryButton}"
                    Click="Create_Click"/>
        </StackPanel>
    </Grid>
</Window>
