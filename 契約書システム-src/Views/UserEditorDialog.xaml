<Window x:Class="ContractManager.Views.UserEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ユーザー編集" 
        MinHeight="450" Width="450"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Username -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="ユーザーID" Style="{StaticResource FormLabel}"/>
            <TextBox x:Name="UsernameBox" Style="{StaticResource ModernTextBox}"/>
        </StackPanel>
        
        <!-- Password -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock x:Name="PasswordLabel" Text="パスワード" Style="{StaticResource FormLabel}"/>
            <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernPasswordBox}"/>
        </StackPanel>
        
        <!-- Name English -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="氏名（英語）" Style="{StaticResource FormLabel}"/>
            <TextBox x:Name="NameEnglishBox" Style="{StaticResource ModernTextBox}"/>
        </StackPanel>
        
        <!-- Birth Date -->
        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Text="生年月日" Style="{StaticResource FormLabel}"/>
            <DatePicker x:Name="BirthDatePicker"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="キャンセル" 
                    Style="{StaticResource SecondaryButton}"
                    Click="Cancel_Click"
                    Margin="0,0,8,0"/>
            <Button Content="保存" 
                    Style="{StaticResource PrimaryButton}"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
